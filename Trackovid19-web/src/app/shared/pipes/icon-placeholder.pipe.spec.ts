import { IconPlaceholderPipe } from './icon-placeholder.pipe';

describe('IconPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new IconPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
