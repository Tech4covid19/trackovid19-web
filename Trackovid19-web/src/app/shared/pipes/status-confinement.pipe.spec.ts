import { StatusConfinementPipe } from './status-confinement.pipe';

describe('StatusConfinementPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusConfinementPipe();
    expect(pipe).toBeTruthy();
  });
});
