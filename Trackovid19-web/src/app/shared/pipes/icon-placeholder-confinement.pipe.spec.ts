import { IconPlaceholderConfinementPipe } from './icon-placeholder-confinement.pipe';

describe('IconPlaceholderConfinementPipe', () => {
  it('create an instance', () => {
    const pipe = new IconPlaceholderConfinementPipe();
    expect(pipe).toBeTruthy();
  });
});
